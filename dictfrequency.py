String =input("Enter a string : ")
def MakeInventory(S):
    list1=S.split()
    for i in range(len(list1)):
        frequency=list1.count(list[i])
    print(frequency)
    for a in range(len(list1[i])):
            key= list1[i]
            value=frequency
            Parameter[key]=value
    print(Parameter)
Parameter={}
MakeInventory(String)
        
    
